3장 데이터구조 : 여러 데이터를 한 변수에 담는게 목적
1절. 리스트 : [타입에 상관없이 0개 이상의 데이터들]
	1.1 리스트 생성 : [], list(range(from, to, step))
	1.2 기본 정보 조회
		- lst[idx]
		- min(lst), max(lst), sum(lst), len(lst), sorted(lst, reverse=True)
	1.3 